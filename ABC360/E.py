from atcoder.modint import Modint, ModContext

mod = 998244353
with ModContext(mod):

    N, K = list(map(int, input().split()))
    one = Modint(1)
    other = Modint(0)

    

    for _ in range(K):
        one_n = Modint(0)
        other_n = Modint(0)

        one_n += 


